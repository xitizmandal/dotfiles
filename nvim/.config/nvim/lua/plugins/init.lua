local essentials = require("plugins.essentials")
local navigations = require("plugins.navigations")
local ui = require("plugins.ui")
local ide = require("plugins.ide")


return {
    essentials,
    navigations,
    ui,
    ide,
}
